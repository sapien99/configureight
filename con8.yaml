# custom script aliases, references by $[echo: xxx]
scripts:
  echo: "echo -n ECHOED"  
  ipify: "curl --silent https://api.ipify.org"  
# allowed python inline functions
functions:
  - int  
# loglevel
loglevel: DEBUG
stacks: true
